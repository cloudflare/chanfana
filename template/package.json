{
  "name": "chanfana-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --config vitest.config.mts",
    "db:migrate:local": "wrangler d1 migrations apply tasks-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tasks-db --remote"
  },
  "dependencies": {
    "chanfana": "^3.0.0",
    "hono": "^4.12.0",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.17",
    "@cloudflare/workers-types": "^4.20260303.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.68.0"
  }
}
